# SQL_more_queries

This project covers MySQL user creation, privilege management,
constraints (PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE), and
retrieving data from multiple tables using JOIN and UNION.
